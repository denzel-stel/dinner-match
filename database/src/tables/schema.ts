import {ingredientsTable, recipesTable} from "./recipes";
import {usersTable} from "./users";

// Tables that are cleared when seeding the database in development.
export default {
    recipesTable,
    ingredientsTable,
    usersTable,
}